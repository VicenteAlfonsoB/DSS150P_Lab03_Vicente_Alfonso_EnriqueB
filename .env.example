# PostgreSQL connection for host-side commands.
# Docker Compose overrides POSTGRES_HOST to the service name "postgres".
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=dss150p
POSTGRES_USER=dss150p
POSTGRES_PASSWORD=change_me
